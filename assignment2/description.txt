代码思路：
先建立两个堆，一个最大堆和一个最小堆，容量都为输入数组的一半
最大堆用于存放所有比当前中位数小的数，最小堆用于存放所有比当前中位数大的数
for循环中，每次新读取一个数，与当前的中位数比较
若比当前的中位数大，则将该数放入最小堆，复杂度为log（k）<= log(n)
同理，若比当前中位数小，则将该数放入最大堆
然后比较两个堆的元素个数，分情况插入和删除堆顶元素。复杂度约为log(k)<= log(n)
所以总时间复杂度为nlog(n)
